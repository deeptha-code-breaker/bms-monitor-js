const BATTERY_STATE = {
  LOW: "low",
  HIGH: "high",
  NORMAL: "normal",
};

const TEMPERATURE_RANGE = {
  low: 0,
  high: 45,
};

const SOC_RANGE = {
  low: 20,
  high: 80,
};

const CHARGE_RANGE = {
  low: 0,
  high: 0.8,
};

export { BATTERY_STATE, TEMPERATURE_RANGE, SOC_RANGE, CHARGE_RANGE };
